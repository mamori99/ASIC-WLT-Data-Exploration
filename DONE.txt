- Use split strategy by wafer/lot instead of random row split (GroupShuffleSplit on wafer_id) to prevent leakage between train and test.
- Add a true holdout set (separate wafers) for final unbiased evaluation; validation set used for threshold tuning/iteration.
- Select decision threshold on validation set using business cost objective (FN×10 + FP×1); threshold no longer hardcoded.
- Add threshold sweep table per target (threshold, recall, precision, FPR, FNR, cost) + cost curve to dashboard; selected operating point highlighted.
- Track business-cost metric (FN×10 + FP×1) on holdout set; added KPI cards to dashboard showing holdout cost, FN/FP breakdown, and threshold.
- Report PR-AUC and recall@0.8precision on holdout set; PR curve now shows holdout data with AUC annotation and recall@precision marker.
- Use stratified group splitting: enumerate all wafer partitions and select the one minimizing fail-rate imbalance across train/val/holdout.
- Audit potential leakage features (x_coord/y_coord): benchmarked with vs without spatial coords. with_xy wins (hot PR-AUC 0.3566 vs 0.3299, cold PR-AUC 0.3543 vs 0.3346, total cost 9138 vs 9388). Spatial position is a legitimate predictive signal — kept.
- Add early stopping (eval_set + early_stopping_rounds=20, n_estimators raised to 500). Early stopping did NOT trigger — val logloss keeps decreasing monotonically across all 4 models (trees used: 498-499/500). Holdout scores regressed vs 100-tree baseline (hot PR-AUC 0.3379 vs 0.3566, cold 0.3164 vs 0.3543) confirming overfitting that logloss cannot detect. Mechanism kept as safety net.
- Remove redundant features: dropped 31 highly correlated feature pairs (|Pearson| > 0.95), 0 near-constant. Features 50 → 19. No regression in holdout scores (identical PR-AUC & cost). Simpler, more interpretable model.
- Tune imbalance handling: compared scale_pos_weight (~24.7) vs compute_sample_weight('balanced'). sample_weight wins on all metrics (hot PR-AUC 0.3379 vs 0.2765, cold 0.3164 vs 0.2617, total cost 9450 vs 9926). Kept sample_weight.
- Perform hyperparameter optimization (Optuna, 50 trials/target, TPE sampler, val cost objective). Optuna beats defaults: hot PR-AUC 0.3824 vs 0.3379 (+0.045), cold PR-AUC 0.3835 vs 0.3164 (+0.067), total cost 9058 vs 9450 (-392). Best params: hot: max_depth=5, lr=0.017, n_est=375, min_child_weight=15, subsample=0.92, colsample=0.56; cold: max_depth=5, lr=0.054, n_est=89, min_child_weight=19, subsample=0.90, colsample=0.43.
- Explore multi-target learning: targets are 92.4% correlated (Pearson). Used leave-one-wafer-out OOF predictions of the other target as a 20th feature. OOF feature ranks #1 in both models. Modest improvement: hot PR-AUC +0.012, total cost -77 (9012→8935). NOT integrated — marginal benefit vs added complexity.
- Add neighbor-based spatial features: k=8 nearest-die mean+std of all 19 features per wafer (cKDTree). 19→49 features after pruning. Negligible improvement: hot PR-AUC +0.002, cold -0.001, total cost -26 (9012→8986). NOT integrated — 2.6× features for zero gain; raw x/y already captures spatial signal.
